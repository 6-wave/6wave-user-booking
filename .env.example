# Copy to .env.local for local work. On Render, set this in the service's Environment tab.
# Leave empty while the app runs on mock data. Public value only: never put secrets
# (Paystack keys included) in NEXT_PUBLIC_* variables.
NEXT_PUBLIC_API_BASE_URL=
